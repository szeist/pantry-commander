// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "hu.szeist.pantrycommander"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
